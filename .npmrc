# @expo/ui ships web-only components (vaul → @radix-ui) that peer-depend on
# react-dom. This is a native-only app, so that peer is never satisfiable and
# never needed. Setting it here — rather than passing --legacy-peer-deps by
# hand — keeps local installs and CI's `npm ci` resolving identically.
legacy-peer-deps=true
